# คู่มือติดตั้งเว็บงานแต่ง เก้า & เบ๊นซ์

เว็บนี้แบ่งเป็น 2 ส่วน ทำตามลำดับนะคะ **ต้องทำส่วนที่ 1 (หลังบ้าน) ก่อน** เพราะขั้นตอนส่วนที่ 2 ต้องใช้ลิงก์จากส่วนที่ 1

- **ส่วนที่ 1 — หลังบ้าน (Google Apps Script + Google Sheet)**: เก็บข้อมูล RSVP, รายจ่าย, รูป/วีดีโอ ฟรี 100%
- **ส่วนที่ 2 — หน้าเว็บ (Netlify หรือ GitHub Pages)**: เว็บไซต์ที่แขกเข้าดู ฟรี 100%

ใช้เวลารวมประมาณ 20-30 นาทีค่ะ

---

## ส่วนที่ 1: ติดตั้งหลังบ้าน (Google Sheet + Apps Script)

### 1.1 สร้าง Google Sheet
1. ไปที่ [sheets.google.com](https://sheets.google.com) แล้วสร้างสเปรดชีตใหม่ (New spreadsheet)
2. ตั้งชื่อว่าอะไรก็ได้ เช่น "เว็บงานแต่ง เก้า เบ๊นซ์ - ข้อมูล"

### 1.2 ใส่โค้ดหลังบ้าน
1. ในสเปรดชีต ไปที่เมนู **Extensions > Apps Script**
2. จะมีหน้าต่างใหม่เปิดขึ้น มีไฟล์ `Code.gs` เปล่าๆ อยู่
3. **ลบโค้ดทั้งหมด** ที่อยู่ในนั้น (Ctrl+A แล้ว Delete)
4. เปิดไฟล์ `apps-script/Code.gs` ที่พริกแกงส่งให้ในนี้ แล้ว **คัดลอกทั้งหมด** (Ctrl+A, Ctrl+C)
5. **วาง (Ctrl+V)** ลงในหน้า Apps Script ที่ว่างอยู่
6. กด **Save** (ไอคอนรูปแผ่นดิสก์ หรือ Ctrl+S)

### 1.3 รัน setup() ครั้งเดียว
1. ที่แถบด้านบนของหน้า Apps Script จะมี dropdown เลือกฟังก์ชัน ให้เลือก **`setup`**
2. กดปุ่ม **Run ▶**
3. จะมี popup ขึ้นมาให้ "Authorize" (อนุญาตสิทธิ์)
   - กด **Review permissions**
   - เลือกบัญชี Gmail ของพี่
   - อาจมีคำเตือน "Google hasn't verified this app" ให้กด **Advanced** > **Go to (ชื่อโปรเจกต์) (unsafe)** — ไม่ต้องกังวลค่ะ เพราะเราเป็นคนเขียนโค้ดเอง ปลอดภัย
   - กด **Allow**
4. รอสักครู่ ระบบจะสร้างชีทให้อัตโนมัติ: `Guests` (พร้อมรายชื่อแขก 165 คนที่ import จาก Excel), `Expenses`, `Gallery`, `Config`
5. เช็คที่แท็บด้านล่างของ Google Sheet ว่ามีชีทครบ 4 อันแล้ว ✅

> ถ้าอยากเปลี่ยน username/password หลังบ้านทีหลัง: ไปที่ Apps Script > ไอคอนเฟือง (Project Settings) > เลื่อนลงไปที่ **Script Properties** > แก้ค่า `ADMIN_USER` / `ADMIN_PASS`

### 1.4 Deploy เป็น Web App
1. ที่มุมขวาบนของหน้า Apps Script กด **Deploy > New deployment**
2. กดไอคอนเฟืองข้างๆ "Select type" แล้วเลือก **Web app**
3. ตั้งค่า:
   - Description: ใส่อะไรก็ได้ เช่น "wedding-api-v1"
   - Execute as: **Me**
   - Who has access: **Anyone**
4. กด **Deploy**
5. จะได้ **Web app URL** หน้าตาประมาณ `https://script.google.com/macros/s/xxxxxxxxxxxxx/exec`
6. **คัดลอก URL นี้เก็บไว้** — ต้องใช้ในขั้นตอนถัดไป

---

## ส่วนที่ 2: เชื่อมหน้าเว็บกับหลังบ้าน

1. เปิดไฟล์ `js/config.js` ด้วยโปรแกรมแก้ไขข้อความ (เช่น Notepad หรือ VS Code)
2. หาบรรทัดนี้:
   ```js
   API_URL: 'PASTE_YOUR_GOOGLE_APPS_SCRIPT_WEB_APP_URL_HERE',
   ```
3. แทนที่ด้วย URL ที่คัดลอกมาจากขั้นตอน 1.4 เช่น:
   ```js
   API_URL: 'https://script.google.com/macros/s/xxxxxxxxxxxxx/exec',
   ```
4. บันทึกไฟล์

---

## ส่วนที่ 3: เอาเว็บขึ้นออนไลน์ (เลือก 1 วิธี)

### วิธี A: GitHub Pages (พี่มีบัญชี GitHub แล้ว ✅ แนะนำวิธีนี้)

1. ไปที่ [github.com](https://github.com) แล้ว login
2. กดปุ่ม **+** มุมขวาบน > **New repository**
3. ตั้งชื่อ repo เช่น `kb-wedding` แล้วกด **Create repository** (ไม่ต้องติ๊กอะไรเพิ่ม)
4. ในหน้า repo ที่สร้างใหม่ กด **uploading an existing file** (หรือ Add file > Upload files)
5. **ลากไฟล์ทั้งหมดในโฟลเดอร์ `wedding-site`** (ทุกไฟล์ ทุกโฟลเดอร์ย่อย css/js/assets) ไปวางในหน้าอัพโหลด
   - ลากทีละโฟลเดอร์ได้เลย เบราว์เซอร์จะรักษาโครงสร้างโฟลเดอร์ให้
6. เลื่อนลงล่าง ใส่ commit message อะไรก็ได้ แล้วกด **Commit changes**
7. ไปที่ **Settings** (แท็บบนสุดของ repo) > เมนูซ้าย **Pages**
8. ที่ "Branch" เลือก **main** และโฟลเดอร์ **/(root)** แล้วกด **Save**
9. รอ 1-2 นาที แล้ว refresh หน้า จะมีลิงก์เว็บขึ้นมา เช่น
   `https://<ชื่อบัญชี>.github.io/kb-wedding/`
10. นี่คือลิงก์เว็บงานแต่งของพี่แล้วค่ะ 🎉 ส่งให้แขกได้เลย

### วิธี B: Netlify Drop (ง่ายกว่า ไม่ต้องมีบัญชีก็ลองได้ก่อน)

1. ไปที่ [app.netlify.com/drop](https://app.netlify.com/drop)
2. ลากทั้งโฟลเดอร์ `wedding-site` ไปวางในหน้าเว็บ
3. รอสักครู่ จะได้ลิงก์เว็บทันที เช่น `https://random-name-12345.netlify.app`
4. ถ้าอยากได้ลิงก์ที่จำง่ายขึ้นและแก้ไขได้ในอนาคต แนะนำสมัครบัญชีฟรีที่ Netlify (กด Sign up) แล้ว "claim" ไซต์ที่เพิ่ง deploy ไป — จะได้แก้ชื่อลิงก์และอัพเดตไฟล์ใหม่ได้ทีหลัง

---

## ทดสอบระบบก่อนส่งให้แขก

1. เปิดลิงก์เว็บที่ได้ → ควรเห็นหน้าการ์ดเชิญ + countdown
2. ลองกรอกฟอร์ม RSVP ทดสอบ 1 รายการ → เช็คว่าเข้า Google Sheet แท็บ `Guests` จริงไหม
3. เปิดหน้า `/gallery.html` → ลองอัพโหลดรูปทดสอบ 1 รูป → เช็คว่ามีไฟล์ใน Google Drive โฟลเดอร์ "เว็บงานแต่ง เก้า&เบ๊นซ์ - Gallery" จริงไหม
4. เปิดหน้า `/admin.html` → login ด้วย `benzkaow` / `Benz@05112011` → เช็คว่าเห็นข้อมูลแขกครบ 165 คน + รายการ RSVP ที่เพิ่งทดสอบ
5. สแกน QR PromptPay ในหน้าแรกด้วยแอปธนาคาร → เช็คว่าขึ้นชื่อ "มงคล นิลวงษ์" ถูกต้อง (**ไม่ต้องกดโอนจริงตอนทดสอบ** แค่เช็คว่าชื่อขึ้นถูก)

---

## แก้ไขเนื้อหาเว็บภายหลัง

- แก้ชื่อ/วันที่/สถานที่/ธีมสี → แก้ในไฟล์ `js/config.js`
- แก้รูปการ์ด → เปลี่ยนไฟล์ใน `assets/invitation-front.jpg`
- แก้ดีไซน์/สี → แก้ในไฟล์ `css/style.css` (ตัวแปรสีอยู่บนสุดของไฟล์)
- หลังแก้ไฟล์แล้ว ถ้าใช้ GitHub Pages: อัพโหลดไฟล์ที่แก้กลับไปทับใน repo (Add file > Upload files) ระบบจะอัพเดตเว็บให้อัตโนมัติใน 1-2 นาที
- ถ้าใช้ Netlify: ลาก-วางโฟลเดอร์ใหม่ทับที่หน้า site เดิมใน Netlify dashboard

## ข้อจำกัดที่ควรรู้ (ของฟรีทุกตัว)

- Google Drive ฟรี 15GB ต่อบัญชี Gmail (เพียงพอสำหรับรูป/วีดีโอหลายร้อยไฟล์)
- วีดีโอที่อัพโหลดจำกัดไว้ที่ 60MB/ไฟล์ รูปภาพ 12MB/ไฟล์ (ปรับได้ในไฟล์ `apps-script/Code.gs` ฟังก์ชัน `uploadMedia_`)
- Apps Script มีโควต้าการใช้งานต่อวัน (สำหรับงานแต่งงานขนาดนี้เพียงพอมากค่ะ ไม่ต้องกังวล)
- Admin session จะหมดอายุทุก 6 ชั่วโมง ต้อง login ใหม่ (ปรับได้ที่ตัวแปร `SESSION_HOURS` ใน Code.gs)
